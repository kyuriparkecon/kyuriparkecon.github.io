# Kyuri Park Personal Website

This is a Quarto website intended for publication with GitHub Pages.

## Local preview

```powershell
quarto preview
```

## Render

```powershell
quarto render
```

## Publish

Push to the `main` branch. The GitHub Actions workflow in
`.github/workflows/publish.yml` renders the site and publishes it to the
`gh-pages` branch.
